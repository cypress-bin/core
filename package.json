{
  "name": "@cypress-bin/entrypoint",
  "license": "MIT",
  "dependencies": {
    "adm-zip": "^0.5.9",
    "cachedir": "^2.3.0",
    "node-fetch": "^2.6.6",
    "rimraf": "^3.0.2",
    "yargs": "^17.3.0"
  },
  "description": "This package is dedicated for users that are behind a firewall, and have access only to npm registry.",
  "peerDependencies": {
    "@cypress-bin/binary-linux-x64": "9.1.1"
  }
}
